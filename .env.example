# Copy to .env and fill in.  .env is git-ignored — never commit real secrets.
# Leave blank to run GENIEPOLIS in Demo fallback mode (fully functional).

DATABRICKS_HOST=
DATABRICKS_TOKEN=
GENIE_SPACE_ID=
